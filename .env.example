# Copy to .env and fill in your key, or just export in your shell:
#   export ANTHROPIC_API_KEY=sk-ant-...
#
# The sandbox script reads this from your environment — it is NEVER baked
# into the Docker image or written to disk inside the container.

ANTHROPIC_API_KEY=sk-ant-your-key-here
